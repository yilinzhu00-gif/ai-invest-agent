# Rollback

Roll back the application to the previously verified immutable image digest. Keep database migrations forward-compatible; enter read-only maintenance and restore only from an operator-verified backup when data integrity is at risk.
